#   DevTinder

-Create a Vite + React application
-Remove unnecessary code and create a Hello World app
-Install Talwind CSS
-Install Daisy UI
-Add NavBar to App.jsx file.
-Create a NavBAr.jsx separate component file
-Install react router dom
-Create BrowserRouter,Routed,Route, in Body of children
-Create an outlook i  our body component.
-Create a footer
-Create a login Page
-Install axios
-CORS-Install cors in backend => add middlewares to with configuration: origin, credentials:true
-Whenever you are  making the API call so pass axios => {withCredentials:true}
-Install react-redux + @redusjs/toolkit- 
-ConfigureStore=>Provider=>createSlice=>Add reducer to store.
-Add redux devtools in chrome
-Login and see if the data is coming properly in the store
-Navbar should update as soon as logs in.
-Refactor our code to add constants file + create a component folder.
-You should not be access other routes without login
-If token is not present redirect user to the login page.
-Logout features
-Get the feed and add the feed in the store.
-Build the user card on field
-Edit profile feature
-Show toast message on save of profile
-New Page-See all my connections
-New Page-Accept/Reject connection request

Remaining:
-Send or ignore the user card from feed
-SignUp new user
-E2E Testing.






Body 
 NavBAr
 Route /Feed
 Route /Login
 Route/ Connection
 Route/ Profile
